# rcli

```bash
cargo run -- csv -i assets/juventus.csv -o output
cargo run -- genpass -l 32
cargo run -- crypto encrypt -k 0123456789ABCDEFGHIJKLMNOPQRSTUVX
cargo run -- base64 encode -i hello!
cargo run -- base64 decode -i aGVsbG8h
```
